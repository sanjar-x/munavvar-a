---
phase: 02-module-convention-database-foundation
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/modules/orders/services.py
  - src/modules/inventory/services.py
  - src/application/client/service.py
  - src/application/client/dependencies.py
  - src/modules/orders/dependencies.py
  - src/modules/inventory/dependencies.py
  - src/api/v1/backoffice/catalog.py
  - src/api/v1/client/catalog.py
  - src/api/v1/courier/catalog.py
  - src/modules/users/schemas.py
  - src/modules/users/queries.py
  - src/modules/inventory/schemas.py
  - src/application/client/schemas.py
  - src/core/seeder.py
  - tests/integration/conftest.py
autonomous: true
requirements: [ARCH-02]

must_haves:
  truths:
    - "No file outside src/modules/catalog/ imports from catalog.services, catalog.enums, or catalog.repositories directly (except Category 3-7 exceptions)"
    - "All consumers of CatalogService import it from src.modules.catalog.public"
    - "All consumers of ProductType import it from src.modules.catalog.public"
    - "All Phase 1 characterization tests pass after consumer updates"
  artifacts:
    - path: "src/modules/orders/services.py"
      provides: "OrderService importing CatalogService from public"
      contains: "from src.modules.catalog.public import"
    - path: "tests/integration/conftest.py"
      provides: "Test fixtures importing ProductType from public"
      contains: "from src.modules.catalog.public import ProductType"
  key_links:
    - from: "src/modules/orders/services.py"
      to: "src/modules/catalog/public.py"
      via: "imports CatalogService from facade"
      pattern: "from src.modules.catalog.public import CatalogService"
    - from: "tests/integration/conftest.py"
      to: "src/modules/catalog/public.py"
      via: "imports ProductType from facade"
      pattern: "from src.modules.catalog.public import ProductType"
---

<objective>
Update all consumers of Catalog module internals to import from the public.py facade, proving the facade pattern end-to-end.

Purpose: Completes ARCH-02 by ensuring no module imports Catalog internals directly. This proves the public.py facade pattern works in practice before it's replicated for other modules in Phases 3-6. Per D-03 and D-04, ALL existing consumers (including test fixtures) are updated.
Output: 17 import statements redirected from catalog internals to catalog.public. Full test suite green.
</objective>

<execution_context>
@C:/Users/Sanjar/Desktop/munavvar-a/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Sanjar/Desktop/munavvar-a/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-module-convention-database-foundation/02-01-SUMMARY.md

<interfaces>
<!-- The facade created in Plan 01 that consumers now import from -->

From src/modules/catalog/public.py (created in Plan 01):
```python
from src.modules.catalog.dtos import ProductDTO
from src.modules.catalog.enums import ProductType
from src.modules.catalog.services import CatalogService

__all__ = [
    "CatalogService",
    "ProductDTO",
    "ProductType",
]
```

Consumer update inventory from RESEARCH.md:

Category 1 (CatalogService imports — 9 files):
  src/modules/orders/services.py
  src/modules/inventory/services.py
  src/application/client/service.py
  src/application/client/dependencies.py
  src/modules/orders/dependencies.py
  src/modules/inventory/dependencies.py
  src/api/v1/backoffice/catalog.py
  src/api/v1/client/catalog.py
  src/api/v1/courier/catalog.py

Category 2 (ProductType imports — 9 files):
  src/modules/users/schemas.py
  src/modules/users/queries.py
  src/modules/inventory/schemas.py
  src/application/client/schemas.py
  src/api/v1/backoffice/catalog.py
  src/api/v1/client/catalog.py
  src/api/v1/courier/catalog.py
  src/core/seeder.py
  tests/integration/conftest.py

DO NOT UPDATE (per research):
  Category 3: Pydantic schema imports (from catalog.schemas) — KEEP as-is
  Category 4: Dependency factory imports (from catalog.dependencies) — KEEP as-is
  Category 5: ProductRepository imports in application UoWs — DEFER to Phase 6
  Category 6: Product ORM model imports in model registry/repos — KEEP as-is
  Category 7: TYPE_CHECKING imports of Product model — KEEP as-is
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all CatalogService and ProductType consumer imports to use public.py</name>
  <files>
    src/modules/orders/services.py,
    src/modules/inventory/services.py,
    src/application/client/service.py,
    src/application/client/dependencies.py,
    src/modules/orders/dependencies.py,
    src/modules/inventory/dependencies.py,
    src/api/v1/backoffice/catalog.py,
    src/api/v1/client/catalog.py,
    src/api/v1/courier/catalog.py,
    src/modules/users/schemas.py,
    src/modules/users/queries.py,
    src/modules/inventory/schemas.py,
    src/application/client/schemas.py,
    src/core/seeder.py,
    tests/integration/conftest.py
  </files>
  <read_first>
    - src/modules/catalog/public.py
    - src/modules/orders/services.py
    - src/modules/inventory/services.py
    - src/application/client/service.py
    - src/application/client/dependencies.py
    - src/modules/orders/dependencies.py
    - src/modules/inventory/dependencies.py
    - src/api/v1/backoffice/catalog.py
    - src/api/v1/client/catalog.py
    - src/api/v1/courier/catalog.py
    - src/modules/users/schemas.py
    - src/modules/users/queries.py
    - src/modules/inventory/schemas.py
    - src/application/client/schemas.py
    - src/core/seeder.py
    - tests/integration/conftest.py
  </read_first>
  <action>
Per D-03 and D-04: Update all existing consumers of Catalog internals to import from `src.modules.catalog.public`.

**Category 1: CatalogService imports (9 files)**

In each of these files, find and replace the import line:
```
from src.modules.catalog.services import CatalogService
```
with:
```
from src.modules.catalog.public import CatalogService
```

Files to update:
1. `src/modules/orders/services.py`
2. `src/modules/inventory/services.py`
3. `src/application/client/service.py`
4. `src/application/client/dependencies.py`
5. `src/modules/orders/dependencies.py`
6. `src/modules/inventory/dependencies.py`
7. `src/api/v1/backoffice/catalog.py`
8. `src/api/v1/client/catalog.py`
9. `src/api/v1/courier/catalog.py`

**Category 2: ProductType enum imports (9 files)**

In each of these files, find and replace the import line:
```
from src.modules.catalog.enums import ProductType
```
with:
```
from src.modules.catalog.public import ProductType
```

Files to update:
1. `src/modules/users/schemas.py`
2. `src/modules/users/queries.py`
3. `src/modules/inventory/schemas.py`
4. `src/application/client/schemas.py`
5. `src/api/v1/backoffice/catalog.py` (may already have CatalogService from public; combine into single import)
6. `src/api/v1/client/catalog.py` (same — combine)
7. `src/api/v1/courier/catalog.py` (same — combine)
8. `src/core/seeder.py`
9. `tests/integration/conftest.py`

**IMPORTANT for files with BOTH CatalogService AND ProductType imports (backoffice/catalog.py, client/catalog.py, courier/catalog.py):**
Combine into a single import line:
```python
from src.modules.catalog.public import (
    CatalogService,
    ProductType,
)
```
Remove BOTH old import lines (`from src.modules.catalog.services import ...` and `from src.modules.catalog.enums import ...`).

**DO NOT TOUCH these imports (keep as-is):**
- `from src.modules.catalog.schemas import ...` — Pydantic schemas stay as direct imports (Category 3)
- `from src.modules.catalog.dependencies import ...` — Dependency factories stay as direct imports (Category 4)
- `from src.modules.catalog.repositories import ...` in application UoWs — Deferred to Phase 6 (Category 5)
- `from src.modules.catalog.models import ...` or `from src.infrastructure.database.models import ... Product ...` — ORM model imports stay (Category 6)
- TYPE_CHECKING imports of `Product` — Stay as-is (Category 7)

After all edits, run ruff format on all modified files:
```bash
uv run ruff format src/modules/orders/services.py src/modules/inventory/services.py src/application/client/service.py src/application/client/dependencies.py src/modules/orders/dependencies.py src/modules/inventory/dependencies.py src/api/v1/backoffice/catalog.py src/api/v1/client/catalog.py src/api/v1/courier/catalog.py src/modules/users/schemas.py src/modules/users/queries.py src/modules/inventory/schemas.py src/application/client/schemas.py src/core/seeder.py tests/integration/conftest.py
```
  </action>
  <verify>
    <automated>cd C:/Users/Sanjar/Desktop/munavvar-a && grep -rn "from src.modules.catalog.services import" src/ tests/ --include="*.py" | grep -v "__pycache__" | grep -v "catalog/public.py" && echo "FAIL: Still has direct catalog.services imports" || echo "OK: No direct catalog.services imports outside catalog/" && grep -rn "from src.modules.catalog.enums import" src/ tests/ --include="*.py" | grep -v "__pycache__" | grep -v "catalog/" && echo "FAIL: Still has direct catalog.enums imports outside catalog/" || echo "OK: No direct catalog.enums imports outside catalog/"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "from src.modules.catalog.services import" src/ tests/ --include="*.py"` only returns hits inside `src/modules/catalog/` itself (public.py, services.py)
    - `grep -rn "from src.modules.catalog.enums import" src/ tests/ --include="*.py"` only returns hits inside `src/modules/catalog/` itself (dtos.py, models.py, enums.py, schemas.py, repositories.py)
    - `grep -rn "from src.modules.catalog.public import" src/ tests/ --include="*.py"` returns 12+ hits (the updated consumers)
    - No import errors when loading the application: `python -c "from src.api.server import create_app; print('App loads OK')"`
  </acceptance_criteria>
  <done>All 17 import statements across 15 files redirected from catalog internals to catalog.public. No module outside catalog/ imports from catalog.services or catalog.enums directly.</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite to verify facade + migration integration</name>
  <files></files>
  <read_first>
    - tests/integration/conftest.py
    - tests/conftest.py
  </read_first>
  <action>
This is a verification task that proves all three plans (DTO/facade, migration rebuild, consumer updates) work together.

1. Run ruff linter on all modified files from this plan AND Plan 01:
```bash
uv run ruff check src/common/types.py src/common/service.py src/modules/catalog/dtos.py src/modules/catalog/public.py src/modules/catalog/services.py src/modules/orders/services.py src/modules/inventory/services.py src/application/client/service.py src/application/client/dependencies.py src/modules/orders/dependencies.py src/modules/inventory/dependencies.py src/api/v1/backoffice/catalog.py src/api/v1/client/catalog.py src/api/v1/courier/catalog.py src/modules/users/schemas.py src/modules/users/queries.py src/modules/inventory/schemas.py src/application/client/schemas.py src/core/seeder.py tests/integration/conftest.py
```
Fix any lint errors.

2. Run the full integration test suite:
```bash
uv run pytest tests/integration/ -x -v
```
All 4 characterization tests must pass:
- test_delivery_fulfillment.py
- test_warehouse_pickup.py
- test_walkin_sale.py
- test_courier_shift_close.py

3. Verify the facade import contract is enforced:
```bash
# Verify no direct catalog.services imports outside catalog/
grep -rn "from src.modules.catalog.services import" src/ tests/ --include="*.py" | grep -v "__pycache__" | grep -v "src/modules/catalog/"

# Verify no direct catalog.enums imports outside catalog/
grep -rn "from src.modules.catalog.enums import" src/ tests/ --include="*.py" | grep -v "__pycache__" | grep -v "src/modules/catalog/"
```
Both greps should return NO results (empty output).

4. Verify the application starts:
```bash
python -c "from src.api.server import create_app; print('App loads OK')"
```

If any test fails, diagnose and fix the root cause. Common issues:
- Import path not updated in a consumer file -> find and fix
- ORM object expected where DTO is now returned -> the service method was called by a consumer that needs the ORM; check if it's a BaseService inherited method (those still return ORM)
- Database connection issue from migration rebuild -> verify `uv run alembic upgrade head` and init_data both completed in Plan 02
  </action>
  <verify>
    <automated>cd C:/Users/Sanjar/Desktop/munavvar-a && uv run ruff check src/common/types.py src/common/service.py src/modules/catalog/dtos.py src/modules/catalog/public.py src/modules/catalog/services.py && uv run pytest tests/integration/ -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ruff check` passes with no errors on all new/modified files
    - `uv run pytest tests/integration/ -x -v` passes all 4 characterization tests
    - `grep -rn "from src.modules.catalog.services import" src/ tests/ --include="*.py" | grep -v "__pycache__" | grep -v "src/modules/catalog/"` returns empty
    - `grep -rn "from src.modules.catalog.enums import" src/ tests/ --include="*.py" | grep -v "__pycache__" | grep -v "src/modules/catalog/"` returns empty
    - `python -c "from src.api.server import create_app"` succeeds
  </acceptance_criteria>
  <done>Full test suite passes. Facade import contract verified. Application loads successfully. Phase 2 requirements ARCH-02, ARCH-03, DB-01, DB-02 are all satisfied.</done>
</task>

</tasks>

<verification>
1. Facade contract: No module outside catalog/ imports from catalog.services or catalog.enums
2. All tests green: `uv run pytest tests/integration/ -x -v` passes all 4 characterization tests
3. App loads: `python -c "from src.api.server import create_app"` succeeds
4. Ruff clean: `uv run ruff check` passes on all modified files
</verification>

<success_criteria>
- 17 import statements redirected to catalog.public across 15 files
- No direct imports of catalog.services or catalog.enums from outside catalog/
- All 4 Phase 1 characterization tests pass
- Application loads without import errors
- Ruff linter passes on all modified files
</success_criteria>

<output>
After completion, create `.planning/phases/02-module-convention-database-foundation/02-03-SUMMARY.md`
</output>
