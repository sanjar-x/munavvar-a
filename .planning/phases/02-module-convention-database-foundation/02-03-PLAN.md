---
phase: 02-module-convention-database-foundation
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/modules/orders/services.py
  - src/modules/inventory/services.py
  - src/application/client/service.py
  - src/application/client/dependencies.py
  - src/modules/orders/dependencies.py
  - src/modules/inventory/dependencies.py
  - src/api/v1/backoffice/catalog.py
  - src/api/v1/client/catalog.py
  - src/api/v1/courier/catalog.py
  - src/modules/users/schemas.py
  - src/modules/users/queries.py
  - src/modules/inventory/schemas.py
  - src/application/client/schemas.py
  - src/core/seeder.py
  - tests/integration/conftest.py
autonomous: true
requirements: [ARCH-02]

must_haves:
  truths:
    - "No file outside src/modules/catalog/ imports from src.modules.catalog.services directly"
    - "No file outside src/modules/catalog/ imports from src.modules.catalog.enums directly (except internal catalog files)"
    - "All CatalogService consumers import from src.modules.catalog.public"
    - "All ProductType consumers (outside catalog) import from src.modules.catalog.public"
    - "Characterization tests pass with updated imports"
    - "Application startup works (FastAPI server starts without import errors)"
  artifacts:
    - path: "src/modules/orders/services.py"
      provides: "CatalogService imported from catalog.public"
      contains: "from src.modules.catalog.public import CatalogService"
    - path: "src/api/v1/backoffice/catalog.py"
      provides: "CatalogService and ProductType imported from catalog.public"
      contains: "from src.modules.catalog.public import"
    - path: "tests/integration/conftest.py"
      provides: "ProductType imported from catalog.public"
      contains: "from src.modules.catalog.public import ProductType"
  key_links:
    - from: "src/modules/orders/services.py"
      to: "src/modules/catalog/public.py"
      via: "import CatalogService"
      pattern: "from src.modules.catalog.public import CatalogService"
    - from: "src/api/v1/backoffice/catalog.py"
      to: "src/modules/catalog/public.py"
      via: "import CatalogService, ProductType"
      pattern: "from src.modules.catalog.public import"
    - from: "tests/integration/conftest.py"
      to: "src/modules/catalog/public.py"
      via: "import ProductType"
      pattern: "from src.modules.catalog.public import ProductType"
---

<objective>
Update all 18 import statements across 13 files to import CatalogService and ProductType from `src.modules.catalog.public` instead of their internal module paths.

Purpose: Completes ARCH-02 end-to-end by proving the facade pattern works in practice. After this plan, no code outside the catalog module directly imports from `catalog.services` or `catalog.enums`. This establishes the import discipline that pytest-archon will enforce in Phase 6.

Output: 18 import statements updated across 13 files (9 CatalogService imports + 9 ProductType imports). All characterization tests still pass. No file outside catalog imports from catalog internals (except the explicitly allowed exceptions: schemas, dependencies, model registry, TYPE_CHECKING, and deferred application-layer UoWs).
</objective>

<execution_context>
@C:/Users/Sanjar/Desktop/munavvar-a/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Sanjar/Desktop/munavvar-a/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-module-convention-database-foundation/02-CONTEXT.md
@.planning/phases/02-module-convention-database-foundation/02-RESEARCH.md
@.planning/phases/02-module-convention-database-foundation/02-01-SUMMARY.md

<interfaces>
<!-- The facade created by Plan 01 that consumers will now import from -->

From src/modules/catalog/public.py (created in Plan 01):
```python
"""Catalog module public API."""
from src.modules.catalog.dtos import ProductDTO
from src.modules.catalog.enums import ProductType
from src.modules.catalog.services import CatalogService

__all__ = [
    "CatalogService",
    "ProductDTO",
    "ProductType",
]
```

<!-- Imports that STAY as-is (NOT updated -- see research Categories 3-8) -->
Categories kept unchanged:
- Category 3: Pydantic schema imports (ProductCreate, ProductResponse, ProductUpdate) -- API layer concerns
- Category 4: Dependencies imports (get_catalog_service) -- FastAPI DI wiring
- Category 5: ProductRepository imports in application UoWs -- DEFERRED to Phase 6
- Category 6: Product ORM model in model registry -- infrastructure concern
- Category 7: TYPE_CHECKING imports of Product model -- SQLAlchemy relationship hints
- Category 8: Internal catalog imports -- module-internal
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all 9 CatalogService import paths to catalog.public</name>
  <files>
    src/modules/orders/services.py,
    src/modules/inventory/services.py,
    src/application/client/service.py,
    src/application/client/dependencies.py,
    src/modules/orders/dependencies.py,
    src/modules/inventory/dependencies.py,
    src/api/v1/backoffice/catalog.py,
    src/api/v1/client/catalog.py,
    src/api/v1/courier/catalog.py
  </files>
  <read_first>
    src/modules/orders/services.py,
    src/modules/inventory/services.py,
    src/application/client/service.py,
    src/application/client/dependencies.py,
    src/modules/orders/dependencies.py,
    src/modules/inventory/dependencies.py,
    src/api/v1/backoffice/catalog.py,
    src/api/v1/client/catalog.py,
    src/api/v1/courier/catalog.py,
    src/modules/catalog/public.py
  </read_first>
  <action>
    In each of the 9 files below, find the line:
    ```python
    from src.modules.catalog.services import CatalogService
    ```
    and replace it with:
    ```python
    from src.modules.catalog.public import CatalogService
    ```

    The exact files and their current import lines:

    **1. `src/modules/orders/services.py`**
    Change: `from src.modules.catalog.services import CatalogService`
    To: `from src.modules.catalog.public import CatalogService`

    **2. `src/modules/inventory/services.py`**
    Change: `from src.modules.catalog.services import CatalogService`
    To: `from src.modules.catalog.public import CatalogService`

    **3. `src/application/client/service.py`**
    Change: `from src.modules.catalog.services import CatalogService`
    To: `from src.modules.catalog.public import CatalogService`

    **4. `src/application/client/dependencies.py`**
    Change: `from src.modules.catalog.services import CatalogService`
    To: `from src.modules.catalog.public import CatalogService`

    **5. `src/modules/orders/dependencies.py`**
    Change: `from src.modules.catalog.services import CatalogService`
    To: `from src.modules.catalog.public import CatalogService`

    **6. `src/modules/inventory/dependencies.py`**
    Change: `from src.modules.catalog.services import CatalogService`
    To: `from src.modules.catalog.public import CatalogService`

    **7. `src/api/v1/backoffice/catalog.py`**
    Change: `from src.modules.catalog.services import CatalogService`
    To: `from src.modules.catalog.public import CatalogService`
    NOTE: This file also imports ProductType -- that is handled in Task 2. If the file has a single combined import like `from src.modules.catalog.services import CatalogService`, just change the services part. The enums import is a separate line.

    **8. `src/api/v1/client/catalog.py`**
    Change: `from src.modules.catalog.services import CatalogService`
    To: `from src.modules.catalog.public import CatalogService`

    **9. `src/api/v1/courier/catalog.py`**
    Change: `from src.modules.catalog.services import CatalogService`
    To: `from src.modules.catalog.public import CatalogService`

    DO NOT touch any other imports in these files. Specifically:
    - Keep `from src.modules.catalog.schemas import ...` as-is (Category 3)
    - Keep `from src.modules.catalog.dependencies import get_catalog_service` as-is (Category 4)
    - Keep all non-catalog imports as-is

    After all edits, verify no import errors:
    ```bash
    uv run python -c "
    from src.modules.orders.services import BaseOrderService
    from src.modules.inventory.services import InventoryService
    from src.application.client.service import ClientService
    print('Service imports OK')
    "
    ```
  </action>
  <verify>
    <automated>cd C:/Users/Sanjar/Desktop/munavvar-a && grep -rn "from src.modules.catalog.services import CatalogService" src/ --include="*.py" | grep -v "src/modules/catalog/" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "from src.modules.catalog.services import CatalogService" src/ --include="*.py" | grep -v "src/modules/catalog/"` returns 0 lines (no external file imports CatalogService from services directly)
    - `grep -rn "from src.modules.catalog.public import CatalogService" src/ --include="*.py" | wc -l` returns exactly 9
    - Each of the 9 files listed above contains `from src.modules.catalog.public import CatalogService`
  </acceptance_criteria>
  <done>All 9 CatalogService consumers outside catalog/ now import from catalog.public. Zero files outside catalog import CatalogService from catalog.services.</done>
</task>

<task type="auto">
  <name>Task 2: Update all 9 ProductType import paths to catalog.public and run full test suite</name>
  <files>
    src/modules/users/schemas.py,
    src/modules/users/queries.py,
    src/modules/inventory/schemas.py,
    src/application/client/schemas.py,
    src/api/v1/backoffice/catalog.py,
    src/api/v1/client/catalog.py,
    src/api/v1/courier/catalog.py,
    src/core/seeder.py,
    tests/integration/conftest.py
  </files>
  <read_first>
    src/modules/users/schemas.py,
    src/modules/users/queries.py,
    src/modules/inventory/schemas.py,
    src/application/client/schemas.py,
    src/api/v1/backoffice/catalog.py,
    src/api/v1/client/catalog.py,
    src/api/v1/courier/catalog.py,
    src/core/seeder.py,
    tests/integration/conftest.py,
    src/modules/catalog/public.py
  </read_first>
  <action>
    In each of the 9 files below, find the line:
    ```python
    from src.modules.catalog.enums import ProductType
    ```
    and replace it with:
    ```python
    from src.modules.catalog.public import ProductType
    ```

    The exact files:

    **1. `src/modules/users/schemas.py`**
    Change: `from src.modules.catalog.enums import ProductType`
    To: `from src.modules.catalog.public import ProductType`

    **2. `src/modules/users/queries.py`**
    Change: `from src.modules.catalog.enums import ProductType`
    To: `from src.modules.catalog.public import ProductType`

    **3. `src/modules/inventory/schemas.py`**
    Change: `from src.modules.catalog.enums import ProductType`
    To: `from src.modules.catalog.public import ProductType`

    **4. `src/application/client/schemas.py`**
    Change: `from src.modules.catalog.enums import ProductType`
    To: `from src.modules.catalog.public import ProductType`

    **5. `src/api/v1/backoffice/catalog.py`**
    Change: `from src.modules.catalog.enums import ProductType`
    To: `from src.modules.catalog.public import ProductType`
    NOTE: This file was also updated in Task 1 for CatalogService. After both tasks, this file should have TWO imports from catalog.public. If the file already had both CatalogService and ProductType in the same import from catalog.public (merged by Task 1), just ensure ProductType is there. If they are separate lines, you may merge them:
    ```python
    from src.modules.catalog.public import (
        CatalogService,
        ProductType,
    )
    ```
    Use your judgment -- Ruff's import sorting (rule I) will handle the final format.

    **6. `src/api/v1/client/catalog.py`**
    Change: `from src.modules.catalog.enums import ProductType`
    To: `from src.modules.catalog.public import ProductType`
    Same merge opportunity with CatalogService import from Task 1.

    **7. `src/api/v1/courier/catalog.py`**
    Change: `from src.modules.catalog.enums import ProductType`
    To: `from src.modules.catalog.public import ProductType`
    Same merge opportunity with CatalogService import from Task 1.

    **8. `src/core/seeder.py`**
    Change: `from src.modules.catalog.enums import ProductType`
    To: `from src.modules.catalog.public import ProductType`

    **9. `tests/integration/conftest.py`**
    Change: `from src.modules.catalog.enums import ProductType`
    To: `from src.modules.catalog.public import ProductType`
    NOTE: This file also imports Product model from `src.infrastructure.database.models` -- that import stays as-is (Category 6 -- model registry for ORM object creation in test fixtures).

    DO NOT touch any of these imports:
    - `from src.modules.catalog.schemas import ...` (Category 3 -- API schemas)
    - `from src.modules.catalog.dependencies import ...` (Category 4 -- DI wiring)
    - `from src.modules.catalog.repositories import ProductRepository` in application UoWs (Category 5 -- deferred to Phase 6)
    - `from src.infrastructure.database.models import ... Product ...` (Category 6 -- model registry)
    - `from src.modules.catalog.models import Product` under TYPE_CHECKING (Category 7)
    - Any internal catalog imports (Category 8)

    After all edits, run Ruff to fix import sorting:
    ```bash
    uv run ruff check --fix src/modules/users/schemas.py src/modules/users/queries.py src/modules/inventory/schemas.py src/application/client/schemas.py src/api/v1/backoffice/catalog.py src/api/v1/client/catalog.py src/api/v1/courier/catalog.py src/core/seeder.py tests/integration/conftest.py
    ```

    Then run the full test suite to verify nothing is broken:
    ```bash
    uv run pytest tests/ -x -v
    ```
    Both unit tests (from Plan 01) and integration tests (Phase 1 characterization tests) must pass.
  </action>
  <verify>
    <automated>cd C:/Users/Sanjar/Desktop/munavvar-a && grep -rn "from src.modules.catalog.enums import ProductType" src/ tests/ --include="*.py" | grep -v "src/modules/catalog/" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "from src.modules.catalog.enums import ProductType" src/ tests/ --include="*.py" | grep -v "src/modules/catalog/"` returns 0 lines (no external file imports ProductType from enums directly)
    - `grep -rn "from src.modules.catalog.public import" src/ tests/ --include="*.py" | wc -l` returns at least 13 (9 CatalogService + some merged with ProductType, plus standalone ProductType imports)
    - `src/modules/users/schemas.py` contains `from src.modules.catalog.public import ProductType`
    - `src/core/seeder.py` contains `from src.modules.catalog.public import ProductType`
    - `tests/integration/conftest.py` contains `from src.modules.catalog.public import ProductType`
    - `uv run pytest tests/ -x -v` passes all tests (unit + integration)
    - `uv run ruff check src/ tests/` passes with no errors
  </acceptance_criteria>
  <done>All 18 import statements updated (9 CatalogService + 9 ProductType). Zero files outside catalog/ import from catalog.services or catalog.enums. All tests (unit + integration) pass. Ruff check clean. ARCH-02 facade pattern proven end-to-end.</done>
</task>

</tasks>

<verification>
1. `grep -rn "from src.modules.catalog.services import CatalogService" src/ tests/ --include="*.py" | grep -v "src/modules/catalog/"` -- returns 0 lines
2. `grep -rn "from src.modules.catalog.enums import ProductType" src/ tests/ --include="*.py" | grep -v "src/modules/catalog/"` -- returns 0 lines
3. `grep -rn "from src.modules.catalog.public import" src/ tests/ --include="*.py" | wc -l` -- returns at least 13
4. `uv run pytest tests/ -x -v` -- all unit and integration tests pass
5. `uv run ruff check src/ tests/` -- no linting errors
</verification>

<success_criteria>
- Zero external imports from catalog.services (CatalogService moved to public)
- Zero external imports from catalog.enums (ProductType moved to public)
- All 13 consumer files updated to import from catalog.public
- Unit tests (DTO + facade) pass
- Integration tests (Phase 1 characterization tests) pass
- Ruff check passes with no errors
- The catalog facade pattern is proven end-to-end: create public.py -> update consumers -> all tests green
</success_criteria>

<output>
After completion, create `.planning/phases/02-module-convention-database-foundation/02-03-SUMMARY.md`
</output>
