1. Загрузка from_inventory to_inventory при выводе Transfers чтобы ответ был детальный сейчас выводится просто from_id to_id не понятно откуда куда по UUID
2. При выводе клиентов нет created_at и т д чтобы понять когда он зарегался
3. При удалении Продукта остатки в инвенторях остаются нужно сделать Soft Delete
4. Добавить отдельный API для sttockeeper чтобы ему не было доступно создания накладных для оприходования, инициализации остатков и списание

5) stockeeper может просматривать баланс только своего Склада и его накладные stockeeper не может видеть остальные склады а также он может смотреть все транспортс и их балансы
6) /app/src/modules/users/repositories.py:142: SAWarning: relationship 'StockTransfer.transactions' will copy column stock_transfers.from_id to column stock_transactions.from_id, which conflicts with relationship(s): 'Inventory.outgoing_transactions' (copies inventories.id to stock_transactions.from_id). If this is not the intention, consider if these relationships should be linked with back_populates, or if viewonly=True should be applied to one or more if they are read-only. For the less common case that foreign key constraints are partially overlapping, the orm.foreign() annotation can be used to isolate the columns that should be written towards. To silence this warning, add the parameter 'overlaps="outgoing_transactions"' to the 'StockTransfer.transactions' relationship. (Background on this warning at: https://sqlalche.me/e/21/qzyx) (This warning originated from the `configure_mappers()` process, which was invoked automatically in response to a user-initiated operation.)
   selectinload(self.model.identities)
7) /app/src/modules/users/repositories.py:142: SAWarning: relationship 'StockTransfer.transactions' will copy column stock_transfers.to_id to column stock_transactions.to_id, which conflicts with relationship(s): 'Inventory.incoming_transactions' (copies inventories.id to stock_transactions.to_id). If this is not the intention, consider if these relationships should be linked with back_populates, or if viewonly=True should be applied to one or more if they are read-only. For the less common case that foreign key constraints are partially overlapping, the orm.foreign() annotation can be used to isolate the columns that should be written towards. To silence this warning, add the parameter 'overlaps="incoming_transactions"' to the 'StockTransfer.transactions' relationship. (Background on this warning at: https://sqlalche.me/e/21/qzyx) (This warning originated from the `configure_mappers()` process, which was invoked automatically in response to a user-initiated operation.)
   selectinload(self.model.identities)
8) /app/src/modules/users/repositories.py:142: SAWarning: relationship 'StockTransaction.from_inventory' will copy column inventories.id to column stock_transactions.from_id, which conflicts with relationship(s): 'StockTransfer.transactions' (copies stock_transfers.from_id to stock_transactions.from_id). If this is not the intention, consider if these relationships should be linked with back_populates, or if viewonly=True should be applied to one or more if they are read-only. For the less common case that foreign key constraints are partially overlapping, the orm.foreign() annotation can be used to isolate the columns that should be written towards. To silence this warning, add the parameter 'overlaps="transactions"' to the 'StockTransaction.from_inventory' relationship. (Background on this warning at: https://sqlalche.me/e/21/qzyx) (This warning originated from the `configure_mappers()` process, which was invoked automatically in response to a user-initiated operation.)
   selectinload(self.model.identities)
9) /app/src/modules/users/repositories.py:142: SAWarning: relationship 'StockTransaction.to_inventory' will copy column inventories.id to column stock_transactions.to_id, which conflicts with relationship(s): 'StockTransfer.transactions' (copies stock_transfers.to_id to stock_transactions.to_id). If this is not the intention, consider if these relationships should be linked with back_populates, or if viewonly=True should be applied to one or more if they are read-only. For the less common case that foreign key constraints are partially overlapping, the orm.foreign() annotation can be used to isolate the columns that should be written towards. To silence this warning, add the parameter 'overlaps="transactions"' to the 'StockTransaction.to_inventory' relationship. (Background on this warning at: https://sqlalche.me/e/21/qzyx) (This warning originated from the `configure_mappers()` process, which was invoked automatically in response to a user-initiated operation.)

10) Реализовать API Для раздела финансы